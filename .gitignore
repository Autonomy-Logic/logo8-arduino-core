# Release artefacts: built by tools/ci/build-release.py and published as
# release assets, never committed (the index points at the release URLs).
dist/
autonomylogic-tm4c-*.tar.bz2
logo-upload-*.tar.gz
.DS_Store
